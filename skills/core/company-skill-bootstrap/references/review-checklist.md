# Company Skill Bootstrap Review Overlay

Use this overlay with `../../../references/review-checklist.md`.

## Additional Checks For This Skill

- Confirm skill path uses domain taxonomy: `skills/<domain>/<skill-name>/`.
- Confirm `Activation Triggers` and `Required Output Contract` exist in `SKILL.md`.
- Confirm references are not duplicated unnecessarily between root and local folders.
